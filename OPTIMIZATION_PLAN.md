# 最適化プラン（GitHub Pages 単体運用向け）

## 前提
- このしおりは GitHub Pages 配信で、サーバー同期なし。
- TODO/旅程は「端末ローカルに保存される」ことを意図した仕様。
- UI/雰囲気は現状維持を最優先（Apple風ミニマルな見た目を崩さない）。

## 目的
1. 保守しやすい構造に整理する。
2. 動作を安定化する（壊れにくくする）。
3. 体感の軽さと見やすさを維持・改善する。
4. 将来の機能追加（例: JSONエクスポート）に備える。

## フェーズ1: 安全な整理（見た目・UXを変えない）
- **データと描画の責務分離**
  - `static checklist` / `custom todos` / `custom itinerary` の処理を関数単位で整理。
  - `load -> validate -> render -> persist` の順序を明文化。
- **定数化**
  - localStorageキー、DOMセレクタ、文言を定数化して重複を削減。
- **イベント整理**
  - インライン `onclick` を段階的に `addEventListener` へ移行（既存挙動は維持）。
- **バリデーション**
  - 入力値（時刻/タイトル/テキスト）の長さ・空文字チェックを共通化。

**完了条件**
- 見た目差分なし。
- 既存機能（タブ切替・チェック保持・追加/編集/削除）が同じ操作で動く。

## フェーズ2: コード分割（配信構成はシンプル維持）
- `index.html` から `style.css` / `app.js` へ分離。
- DOM生成ロジックを小関数化（例: `createTodoItem`, `createTimelineItem`）。
- HTML内の重複マークアップをテンプレート化。

**完了条件**
- Lighthouseや体感で初期表示の悪化なし。
- 変更時に触る箇所が明確化される。

## フェーズ3: ローカル保存の品質改善
- **スキーマバージョン管理**
  - 例: `{ version: 1, todos: [...], itinerary: [...] }`。
- **破損データ耐性**
  - 不正JSON時は安全に初期化（壊れたデータで画面が止まらない）。
- **ID生成の衝突回避**
  - 時刻依存 + ランダムの現行方式をUUIDベースに置換検討。
- **将来拡張の足場**
  - 任意で「この端末のデータを書き出す/読み込む」導線を設計（同期はしない）。

**完了条件**
- localStorageが壊れていてもページが利用可能。
- 既存ユーザーデータの移行方針を明文化。

## フェーズ4: UX微調整（雰囲気維持）
- 編集導線を `prompt` から軽量インライン編集へ（モーダル感を減らす）。
- 空状態・入力エラー時のメッセージを統一して見やすく。
- キーボード操作（Enterで追加、Escでキャンセル）対応。

**完了条件**
- 片手操作しやすく、操作回数が増えない。
- 見た目のトーン（色/余白/カード設計）は維持。

## 実施順（推奨）
1. フェーズ1（最小リスク）
2. フェーズ3（データ安全性）
3. フェーズ2（分割）
4. フェーズ4（仕上げ）

## 見積もり（目安）
- フェーズ1: 0.5日
- フェーズ3: 0.5日
- フェーズ2: 0.5〜1日
- フェーズ4: 0.5日

合計: 約2〜3日（レビュー込み）

## 次アクション提案
- まずは**フェーズ1のみ**を1PRで実施し、差分を小さく保って安全に進める。
